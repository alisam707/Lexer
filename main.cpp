//will parse input to check if it's a letter, digit, operator, etc. and call functions

//will also need to separate tokens

#include 'id.cpp'
#include <iostream>
#include<string>

string code = "a6c";

if (input[0] == char) {
	Machine.switchStates(code);

}